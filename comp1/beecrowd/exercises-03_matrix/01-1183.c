#include <stdio.h>

void read(int m[][]) {

}

int main() {
    int m[12][12];    
    char op;
    scanf("%c", &op);
    for(int i = 0; i < 1; i++) 
        for(int j = 0; j < 12; j++) 
            scanf("%d", &m[i][j]);
    if(op == "S") {
        for
    }
  return 0;
}